import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.jacksoneco',
  appName: 'digimon',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
